import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['img.daisyui.com', 'i.ibb.co.com', 'upload.wikimedia.org', 'encrypted-tbn0.gstatic.com', 'www.boipokabd.com', 'res.cloudinary.com', 'ds.rokomari.store'],
  },
};

export default nextConfig;
